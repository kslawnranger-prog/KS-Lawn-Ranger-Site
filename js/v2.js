const header=document.getElementById('siteHeader');const button=document.getElementById('menuButton');const menu=document.getElementById('mobileMenu');const onScroll=()=>header.classList.toggle('scrolled',window.scrollY>45);onScroll();window.addEventListener('scroll',onScroll,{passive:true});button?.addEventListener('click',()=>{const open=menu.classList.toggle('open');button.setAttribute('aria-expanded',String(open))});menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));document.querySelectorAll('[data-compare]').forEach(c=>{const range=c.querySelector('input'),before=c.querySelector('.compare-before'),line=c.querySelector('.compare-line'),handle=c.querySelector('.compare-handle');const update=()=>{const v=range.value+'%';before.style.width=v;line.style.left=v;handle.style.left=v};range.addEventListener('input',update);update()});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();