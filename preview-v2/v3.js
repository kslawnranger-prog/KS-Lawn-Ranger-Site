const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');const links=document.querySelector('.links');const progress=document.querySelector('.page-progress');const heroMedia=document.querySelector('.hero-media img');
const navLinks=[...document.querySelectorAll('.links a[href^="#"]')];const sections=navLinks.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);
const onScroll=()=>{const y=window.scrollY;nav?.classList.toggle('scrolled',y>28);if(progress){const max=document.documentElement.scrollHeight-window.innerHeight;progress.style.width=(max?y/max*100:0)+'%'}if(heroMedia&&y<window.innerHeight*1.15){heroMedia.style.transform=`scale(${1+Math.max(0,.035-y/26000)}) translateY(${y*.08}px)`}let active='';sections.forEach(section=>{if(section.getBoundingClientRect().top<=window.innerHeight*.38)active='#'+section.id});navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===active))};
onScroll();window.addEventListener('scroll',onScroll,{passive:true});
menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});links?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));
document.querySelectorAll('[data-compare]').forEach(c=>{const range=c.querySelector('input'),before=c.querySelector('.before'),line=c.querySelector('.line'),handle=c.querySelector('.handle');const update=()=>{const v=range.value+'%';before.style.width=v;line.style.left=v;handle.style.left=v};range.addEventListener('input',update);update()});
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -5%'});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id&&id.length>1){const target=document.querySelector(id);if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}}}));
const form=document.querySelector('.form');form?.addEventListener('submit',()=>{const button=form.querySelector('button[type="submit"]');if(button){button.classList.add('is-submitting');button.querySelector('span').textContent='Sending request'}});