const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');const links=document.querySelector('.links');window.addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>30),{passive:true});menu?.addEventListener('click',()=>nav.classList.toggle('open'));links?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('[data-compare]').forEach(c=>{const range=c.querySelector('input'),before=c.querySelector('.before'),line=c.querySelector('.line'),handle=c.querySelector('.handle');const update=()=>{const v=range.value+'%';before.style.width=v;line.style.left=v;handle.style.left=v};range.addEventListener('input',update);update()});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.getElementById('year').textContent=new Date().getFullYear();